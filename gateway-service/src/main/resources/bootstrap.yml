spring:
  application:
    name: gateway-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-service
  profiles:
    active: dev
  output:
    ansi:
      enabled: ALWAYS